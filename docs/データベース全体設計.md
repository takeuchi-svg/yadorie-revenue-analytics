# データベース全体設計（正本）

実在する全テーブル/ビューの台帳と、システム横断の前提。**スキーマに関する疑問はまずここ。** 個別機能の背景は各設計書、実行するDDLは `scripts/sql/*.sql`。

> ⚠️ 更新（2026-07 計算監査 `scripts/sql/calc_audit_fixes.sql`）:
> - **KPI定義を人泊・室泊に統一**: mart_monthly_kpi ほか売上分析系の guests=人泊（人数×泊数）、guest_unit=人泊単価、companion=人泊÷室泊。Lincoln系（mart_booking_lt/gs/plan/adr_band）の rooms_total=室泊（部屋数×泊数）、ADR=売上÷室泊。月次帰属はチェックイン月（freee計上基準）を維持。
> - **取消率 = 取消 ÷ 全予約**（取消済み予約も予約としてカウント。予約0件はNULL）。LT系は ABS()→GREATEST(,0)。
> - **mart_monthly_kpi から occ/revpar/revenue_budget/total_inventory 列を廃止**（dim_budget依存の遺物。稼働率は mart_occupancy_* が正）。**mart_daily は削除**（誤った稼働率定義を含んでいたため）。
> - **dim_operating_days に rooms 列追加**（月別客室数の上書き。改装等で部屋数が変わる月に対応。例: FRY 2026-06まで10室→07から13室）。mart_occupancy_* の分母は `COALESCE(od.rooms, dim_facility.total_rooms)` × `COALESCE(od.days, データ日数)`。

- DB: Supabase(PostgreSQL)。命名規約 `raw_`(生データ) / `dim_`(マスタ) / `mart_`(集計VIEW)。例外テーブル（budget_*/actual_monthly/ai_*/app_user/user_facility）は歴史的経緯で無接頭辞。
- 施設キー: `facility` / `work_facility` / `home_facility` = **TEXT のBIコード**（FRY, NS…）。サロゲートID無し。全結合の軸。
- 時間軸: 月=TEXT `'YYYY-MM'`、日付=DATE、勤怠/シフトの労働時間=**分(整数)**。
- **mart_ は全て通常VIEW**（MATERIALIZED VIEWは無い）＝毎クエリで raw を集計する。→ 70施設スケール時はマテリアライズ化を検討（§既知の負債）。

---

## 1. テーブル/ビュー台帳

### raw_（生データ・入力）
| テーブル | キー/UNIQUE | 粒度 | 施設列 | 由来 |
|---|---|---|---|---|
| raw_reservation | UNIQUE(facility, pms_id) | 予約1件 | facility | PMS(Staysee) |
| raw_basic_product | UNIQUEなし（idx facility,pms_id） | 予約×基本商品 | facility | Staysee |
| raw_other_product | UNIQUEなし | 予約×その他商品 | facility | Staysee |
| raw_payment | UNIQUEなし（PKのみ・**索引なし**） | 予約×入金 | facility | Staysee |
| raw_booking_event | UNIQUE(facility, notify_no) | 通知(予約/取消)1件 | facility | サイトコントローラ(Lincoln) |
| raw_rate_snapshot | UNIQUE(facility, snapshot_date, stay_date, scope, COALESCE(room,'')) | スナップ日×宿泊日 | facility | レート表 |
| raw_room_sales | UNIQUE(facility, stay_date, scope, COALESCE(room_type,'')) | 宿泊日×客室タイプ | facility | 販売数集計表（**稼働率の正データ**） |
| raw_attendance_daily | UNIQUE(staff_code, work_date, work_facility) | 従業員×日×施設 | work_facility | 勤怠(Touch On Time)＋スポット手入力(source='manual') |
| raw_shift_plan | UNIQUE(staff_code, work_facility, work_date) | シフト計画1セル(=予) | work_facility | シフト画面 |
| raw_shift_segment | UNIQUE(shift_id, seq) | 計画内の役割セグメント | （親plan経由） | シフト画面 |
| raw_daily_plan_context | PK(facility, work_date) | 日次手入力(オンハンド/予測/メモ) | facility | シフト画面 |
| raw_review | UNIQUE(facility, source, source_review_id) | WEBクチコミ1件（sub_ratings/reviewer_attrはJSONB・原文キー） | facility | じゃらん/一休CSV・手動 |
| raw_review_summary | UNIQUE(facility, source, month) | 楽天の宿カルテ集計値（月次転記） | facility | 手動フォーム |
| raw_survey_response | idx(facility, response_at) | 館内アンケート1回答（v1設問+raw_answers JSONB） | facility | Googleフォーム→CSV |
| raw_feedback_topic | UNIQUE(source_table, source_id, source_field, topic_code, model_version) | AI定性分析の結果（トピック×センチメント） | facility | AIバッチ(C4) |
| raw_improvement_insight | UNIQUE(facility, month, window_months, topic_code, model_version) | 改善レポート（課題/実引用/解決策①②③）のキャッシュ | facility | AIバッチ(C4拡張) |
| raw_seasonality_note | UNIQUE(facility, month 1-12) | 繁閑の理由（暦月・季節性の定性メモ） | facility | 設定/施設プロフィール |
| raw_facility_initiative | idx(facility, year_month) | 取組履歴（事実のみ。主観的成否は持たない） | facility | 設定/施設プロフィール |

### dim_（マスタ）
| テーブル | キー | 用途 |
|---|---|---|
| dim_facility | PK facility | 施設マスタ（name, short_name, total_rooms, rooms_json=レート表用部屋リスト） |
| dim_facility_mapping | PK attendance_code | 勤怠所属コード(数値)→BIコード。**参照/将来用。勤怠パーサーはTS定数(FACILITY_MAP)が実際の正** |
| dim_budget | UNIQUE(facility, month) | **レガシー月別予算**。`mart_monthly_kpi`が参照（budget_monthlyと別系統。§予算参照） |
| dim_ota_marketing | UNIQUE(facility, month, ota, metric) | OTA広告費等（EAV） |
| dim_operating_days | PK(facility, month) | 手入力の稼働日数（在庫数=総室数×稼働日数） |
| dim_productivity_manual | PK(facility, month) | 生産性の備考メモ（旧: 手入力2項目。T13で自動化・列は残置） |
| dim_staff | PK staff_code | 従業員（name, home_facility, employment_type, is_monthly_salary, **is_spot**）。**賃金列は持たない** |
| dim_staff_wage | PK staff_code | **賃金**（wage_type/hourly_wage/monthly_salary/deemed_ot_hours/contracted_monthly_hours）。給与閲覧権限を行RLSで保護 |
| dim_role | identity PK | シフトの役割マスタ（色） |
| dim_shift_pattern | identity PK | 勤務/休日パターン（時刻・色・facility=NULLは全社共通） |
| dim_facility_profile | PK facility | 施設プロフィール（意図・方針・NG・競合。**AI3系統のプロンプトへ自動注入**＝profile-context.ts） |

### 予算・PL・その他（無接頭辞・歴史的経緯）
| テーブル | キー | 用途 |
|---|---|---|
| budget_daily | UNIQUE(facility, **date**) | 日別予算（稼働室 rooms_sold・宿泊人数 guests・各revenue）。**日付列は `date`（work_dateではない）** |
| budget_monthly | UNIQUE(facility, fiscal_year, month, item_code) | 月次予算P&L（EAV） |
| actual_monthly | UNIQUE(facility, fiscal_year, month, item_code) | 月次実績P&L（EAV。actual, prior_amount=昨年） |
| app_user | PK user_id | アプリ利用者（role, **can_view_wage**） |
| user_facility | PK(user_id, facility) | member の閲覧可能施設 |
| ai_summary / ai_issue | UNIQUE(facility, month) | 概要ページのAI生成キャッシュ |

### mart_（集計VIEW・全てplain view）
| ビュー | ソース | 粒度 | 備考 |
|---|---|---|---|
| mart_monthly_kpi | raw_reservation × dim_budget | facility×month | ⚠rooms_sold=SUM(nights)・予算はレガシーdim_budget（§既知の負債） |
| mart_daily | raw_reservation × dim_facility | facility×checkin日 | ⚠連泊をCI日に全計上。日別ページは自前で泊分割 |
| mart_channel_monthly | raw_reservation | ×channel | |
| mart_room_monthly / mart_room_type_monthly | raw_reservation | ×客室/タイプ | |
| mart_residence_monthly | raw_reservation | ×都道府県/国 | |
| mart_plan_monthly / mart_adr_band_monthly / mart_gs_monthly | raw_reservation/raw_booking_event | ×プラン/ADR帯/人数 | |
| mart_meal_monthly | raw_basic_product × raw_reservation | ×喫食タイプ | |
| mart_fb_category | raw_other_product × raw_reservation | ×カテゴリ | |
| mart_cxl_summary / mart_cxl_lt / mart_booking_lt | raw_booking_event | ×channel/LT帯 | |
| mart_occupancy_daily / mart_occupancy_monthly / mart_room_sales_type_monthly | raw_room_sales | 稼働率の正 | month=source_month(ファイル名由来) |
| mart_budget_revenue_monthly | budget_monthly | facility×month | 月次売上予算 |
| mart_labor_monthly | raw_attendance_daily | facility×month | HQ除外。総労働時間・人数・残業（source問わず=スポット含む） |
| mart_shift_variance | raw_shift_plan ⟗ raw_attendance_daily | 従業員×日×施設 | 予実差(実−予)。FULL OUTER JOIN |
| mart_labor_cost_actual / _plan | 勤怠/計画 × dim_staff_wage | 従業員×施設×月 | **給与閲覧権限者のみ**（security_invoker=on） |
| mart_labor_cost_monthly | mart_labor_cost_actual相当 | facility×month | 生産性KPI供給（deemed_ot_excess_pay/spot_hours）。**施設メンバー可**（invoker=off・ビュー内ゲート） |
| mart_daily_plan_context | budget_daily × raw_daily_plan_context | facility×work_date | budget_daily.date→work_date別名 |
| mart_guest_feedback / _3mo | raw_review＋raw_survey_response × dim_axis_mapping | facility×month×channel(web/survey)×axis | ベイズ平滑化(k=10・長期24ヶ月・lt_n<12はNULL)。_3moが主表示 |
| mart_improvement_topics | raw_feedback_topic | facility×month×topic | ネガ言及数降順=改善候補TOP。source_kinds=2でWEB+アンケート両言及 |
| mart_nps | raw_survey_response | facility×month | NPS＋推奨/中立/批判者数 |

---

## 2. 予算の3系統（混同注意）

1. **dim_budget**（レガシー・月別）: `mart_monthly_kpi` の revenue_budget/total_inventory が参照。未メンテだと概要のocc/revpar分母がNULLになる。
2. **budget_monthly**（月次PL）: 予実管理(yojitsu)・`mart_budget_revenue_monthly` が参照。予実の正。
3. **budget_daily**（日別）: 日別売上(daily)・シフトの稼働前提が参照。日付列は `date`。

→ 将来 dim_budget を budget_monthly へ一本化するのが望ましい（§既知の負債）。

## 3. 施設コードとマッピング

- BIコード（FRY等）が全テーブルの施設キー。27施設→2030年70施設へM&A拡大予定。**施設追加は原則データ追加のみ**（`dim_facility`＋`dim_facility_mapping`に行追加）。
- **例外**: 勤怠パーサー `src/lib/etl/attendance-parser.ts` の `FACILITY_MAP`/`NAME_FAC`（TS定数）に施設を足す必要がある＝コード変更＋デプロイ。70施設化前にDB駆動化したい負債。
- 施設コードは2系統: 勤怠所属コード（数値"121"）↔ BIコード("FRY")。変換は `dim_facility_mapping`。
- HQ（本社部門）は施設KPIから除外（`work_facility <> 'HQ'`）。

## 4. システム横断の前提（AIエージェント/実装者向け・◆=誤解しやすい）

- ◆ 稼働率・販売室数の正は **raw_room_sales / mart_occupancy_***。`raw_reservation.room_count` は全件1固定で**使用禁止**。`mart_monthly_kpi.rooms_sold` は SUM(nights) で室数と厳密には異なる（ADRが過大寄り）。
- ◆ 売上は2系統: raw_reservation の精算額（status='C/O', nights>0）と PLの `sales_total`。人件費率はPL同士、生産性は raw_reservation/mart を使い分け。
- ◆ PL(actual_monthly/budget_monthly)は freee由来。**「人件費」カテゴリも「GOP」行も無い**。全費目が `category="売上高"`。人件費=item_name合計（給料手当+賞与+通勤費+法定福利費+福利厚生費+雑給+外注費(人材)、実績は外注費が合算の場合あり）。付加価値≒`gross_profit`（売上−原価）。
- ◆ `fiscal_year` は4月開始（'2026'=2026/4〜2027/3）。売上の月判定は checkin ベース `TO_CHAR(checkin,'YYYY-MM')`。
- ◆ 勤怠のヘルプ勤務は**計上先施設**に計上。時間は分。労働時間の小数時間は×60で分に正規化（Touch On Time）。
- ◆ 人件費: 時給=実働×時給／月給=月給+超過×(月給/所定×1.25)。**割増1.25のみ**。月給baseは**本務施設のみ**計上、残業判定は従業員×月の全施設合計時間（クロス施設二重計上を防止）。
- ◆ スポット判定は `dim_staff.is_spot`（attendanceに列は持たない）。スポット実働は raw_attendance_daily(source='manual')。
- localStorage禁止（施設選択はCookie）。

## 5. 権限モデル（RLS）

- 関数 `is_admin()` / `can_access_facility(f)` / `can_view_wage()`（`scripts/sql/rls_facility.sql`・`wage_permission.sql`）。
- **admin**=全施設・全操作。**member**=`user_facility` の施設のみ読み書き。マスタ(dim_facility等)=全員read・admin書き。
- **賃金(dim_staff_wage)・個人別人件費ビュー**=給与閲覧権限(`app_user.can_view_wage`)者のみ。施設×月合計(mart_labor_cost_monthly)は施設メンバー可。
- mart_ ビューは `security_invoker=on` で閲覧者のRLSを継承（例外: mart_labor_cost_monthly は invoker=off＋ビュー内ゲート）。
- AI API(/api/chat,/api/insight)はBearer認証＋memberは許可施設フィルタをサーバー強制（service_roleがRLSをバイパスするため）。

## 6. 命名の揺れ（既知・統一は将来課題）

| 概念 | 出現する列名 |
|---|---|
| 日付 | date / stay_date / checkin / work_date / snapshot_date |
| 月 | month('YYYY-MM') / source_month / ym(date) |
| 室数 | rooms_sold / rooms / rooms_total / room_count(使用禁止) / total_rooms / inventory / total_inventory |
| 売上 | revenue / revenue_settled / amount_gross / amount / total / actual |
| 施設 | facility / work_facility / home_facility / attendance_code |

## 7. 既知の負債（優先度順・レビュー由来）

1. mart が全て plain VIEW → 70施設×数年でマテリアライズ化。**変換テンプレ＋取込後REFRESH配線は用意済**（`scripts/sql/materialize_marts.sql`＋upload→`refresh_all_marts()`）。遅くなったビューに個別適用する（matviewはRLS不可のため「matview＋施設ゲートview」の2段で）。
2. mart_monthly_kpi の rooms_sold=SUM(nights)・予算=レガシーdim_budget → raw_room_sales/budget_monthly へ寄せる。
3. 明細(basic/other_product/payment)は UNIQUE無し＋ファイル名source_month依存の重複防止 → キー化 or 取込時ガード（現状: source_month不明は取込拒否で暫定対処済）。
4. raw_payment 索引なし・raw_rate_snapshot 保持ポリシーなし（行数爆発）。
5. ~~FACILITY_MAP のTS二重管理~~ → **対応済**: 勤怠取込は `dim_facility_mapping`（DB）を渡す。施設追加はデータのみで完結（TS定数はフォールバック）。
6. クライアント全件集計(revenue/daily/fb) → サーバー側フィルタ/mart化（未対応）。
7. DB行型 → `src/lib/db-types.ts` に集約済（将来 `supabase gen types` で置換可）。データ取得は `useFacilityData` フックに共通化（rate/cancel/fb/ota適用済、重い5ページは今後）。
